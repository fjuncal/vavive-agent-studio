spring:
  datasource:
    url: jdbc:h2:mem:vavive_gptmaker_test;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  flyway:
    enabled: false
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create-drop

app:
  jwt:
    secret: test-secret-with-at-least-32-characters

gptmaker:
  base-url: https://api.gptmaker.ai
  api-token:
  mock-enabled: true

vavive:
  whatsapp:
    provider: dry-run
  webhooks:
    agent:
      secret:
